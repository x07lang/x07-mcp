# Pinned Schema

`server.schema.2025-12-11.json` is the vendored MCP registry schema pin used by
`x07-mcp registry gen` and `x07-mcp publish --dry-run`.

The canonical schema URL is:

- `https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json`
