# MCP Server (X07) — Template

This is a minimal placeholder project scaffolded by `x07-mcp`.

Next steps:

- Add dependencies needed for MCP transports and secure IDs.
- Implement an MCP server loop (stdio or HTTP transport).

