# MCP Server (stdio) — Template

This is a minimal placeholder project scaffolded by `x07-mcp`.

Next steps:

- Add `ext-stdio` and `ext-rand` dependencies (once published) and run `x07 pkg lock`.
- Implement an MCP stdio transport loop (newline-delimited JSON-RPC).

