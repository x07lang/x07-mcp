# MCP Server (Streamable HTTP) — Template

This is a minimal placeholder project scaffolded by `x07-mcp`.

Next steps:

- Add dependencies for Streamable HTTP transport + OAuth resource server behavior.
- Implement MCP HTTP endpoints and authorization.

