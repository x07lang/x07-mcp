# Changelog

## Unreleased

- x07-mcp CLI implemented in X07 (bundled via `x07 bundle`).
- `ext-mcp-*` package updates (0.1.1) for schema validation and sandbox/budget wiring.

## 0.1.0

- Initial release of `ext-mcp-*` packages and stdio server template.

