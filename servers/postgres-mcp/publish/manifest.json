{
  "manifest_version": "0.3",
  "name": "postgres-mcp",
  "display_name": "postgres-mcp",
  "version": "0.1.0",
  "description": "postgres-mcp deterministic MCP reference server",
  "author": {
    "name": "x07"
  },
  "server": {
    "type": "binary",
    "entry_point": "server/postgres-mcp",
    "mcp_config": {
      "command": "${__dirname}/server/postgres-mcp",
      "args": []
    }
  }
}
